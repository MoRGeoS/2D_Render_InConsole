So this project uses GPU Render method

Explanation:
We have two buffers(string type), when first is rendered it goes to show on the screen and second one getting done. After first one is shown pointers just swaping.
